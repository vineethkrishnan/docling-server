# ============================================
# Docling Production Environment Variables
# Copy this file to ../.env and configure
# ============================================
#
# ⚠️  SECURITY WARNING:
# - Never commit this file with real credentials
# - Use strong, unique tokens (min 32 characters)
# - Change all default passwords before deployment
#
# Generate secure tokens with:
#   openssl rand -hex 32
#
# ============================================

# ===========================================
# API Configuration
# ===========================================

# API authentication token (REQUIRED - min 16 chars for production)
# Generate with: openssl rand -hex 32
DOCLING_API_TOKEN=your-secure-api-token-here

# Environment (production/development)
ENV=production

# API host and port
HOST=0.0.0.0
PORT=8000

# Number of API workers
WORKERS=2

# CORS allowed origins (comma-separated)
CORS_ORIGINS=https://docling.ayunis.de

# ===========================================
# Redis Configuration
# ===========================================

REDIS_HOST=redis
REDIS_PORT=6379
# Set a password for production (leave empty for no password)
REDIS_PASSWORD=

# ===========================================
# Celery Worker Configuration
# ===========================================

# Number of concurrent tasks per worker
CELERY_CONCURRENCY=2

# Task queue name
CELERY_QUEUE=docling

# Log level (debug/info/warning/error)
CELERY_LOGLEVEL=info

# Preload embedding models on worker startup
PRELOAD_MODELS=true

# ===========================================
# Embedding Model Configuration
# ===========================================

# Sentence transformer model for embeddings
# Options:
#   - sentence-transformers/all-MiniLM-L6-v2 (fast, 384 dim)
#   - sentence-transformers/all-mpnet-base-v2 (better quality, 768 dim)
#   - sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2 (multilingual)
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# ===========================================
# Flower Monitoring (Optional)
# ===========================================

# Change default credentials for production!
FLOWER_USER=admin
FLOWER_PASSWORD=your-secure-flower-password
FLOWER_PORT=5555

# ===========================================
# SSL/Domain Configuration
# ===========================================

# Your domain (used in nginx config)
DOMAIN=docling.ayunis.de

# Email for Let's Encrypt notifications
LETSENCRYPT_EMAIL=admin@ayunis.de
